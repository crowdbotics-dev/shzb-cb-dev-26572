import React from "react";
import { View } from "react-native";

const Appmenu = () => {
  return <View style={{
    padding: 10,
    position: 'relative',
    height: '100%'
  }}>
        
      </View>;
};

export default Appmenu;